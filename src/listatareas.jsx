import React from "react";
import ReactDOM from "react-dom/client";
import { Main } from "./listatareas/main.jsx";


ReactDOM.createRoot(document.getElementById("root2")).render(
  <React.StrictMode>
    <Main></Main>
  </React.StrictMode>
);